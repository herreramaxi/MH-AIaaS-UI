export enum OperatorType {
    Nop = "Nop",
    Dataset = "Dataset",
    Clean = "Clean",
    Split = "Split",
    Train = "Train",
    Evaluate = "Evaluate",
    Normalize = "Normalize",
    EditDataset = "EditDataset"
}

export enum AuthenticationType {
    NA=0,
    TokenBased = 2,
}