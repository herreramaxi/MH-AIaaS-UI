export enum OperatorType {
    Nop = "Nop",
    Dataset = "Dataset",
    Clean = "Clean",
    Split = "Split",
    Train = "Train",
    Evaluate = "Evaluate",
    Normalize = "Normalize",
    EditDataset = "EditDataset"
}